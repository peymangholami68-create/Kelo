{
  "name": "kelo-marketplace",
  "version": "1.0.0",
  "private": true,
  "description": "KELO agricultural services marketplace - VPS production foundation",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "db:migrate": "node server/migrate.js",
    "admin:create": "node server/create-admin.js",
    "check": "node --check server/index.js && node --check server/auth.js && node --check server/migrate.js && node --check server/create-admin.js && node --check backend/kelo-backend.js && node --check js/app.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "dotenv": "17.4.2",
    "express": "5.2.1",
    "pg": "8.23.0"
  }
}
