{
  "name": "kelo-marketplace",
  "version": "0.3.0",
  "private": true,
  "description": "KELO | بازار خدمات کشاورزی — VPS-ready web application foundation",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "db:migrate": "node scripts/migrate.js",
    "db:status": "node scripts/db-status.js",
    "check": "node --check server/index.js && node --check backend/kelo-backend.js && node --check scripts/migrate.js && node --check scripts/db-status.js && node --check js/app.js"
  },
  "engines": {
    "node": ">=22 <25"
  },
  "dependencies": {
    "cors": "2.8.6",
    "dotenv": "17.4.2",
    "express": "5.2.1",
    "express-rate-limit": "8.7.0",
    "helmet": "8.3.0",
    "pg": "8.23.0"
  }
}
