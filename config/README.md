# Configuration

`kelo-config.js` intentionally contains only non-secret/public configuration.
For Vercel Production, put runtime public values in the deployment environment and avoid committing credentials.
