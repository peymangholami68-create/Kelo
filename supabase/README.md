# Supabase (not active in VPS target)

The current Kelo production target is a self-hosted VPS + Node/Express + PostgreSQL architecture.

This folder is retained as historical architecture material from the previous Supabase design and is not used by the current runtime.
